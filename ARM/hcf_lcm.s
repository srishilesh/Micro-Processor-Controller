 AREA LCM,CODE,READONLY
 ENTRY
 MOV R1,#10
 MOV R0,#2
 MUL R3,R0,R1
 MOV R4,R3
 MOV R5,#0

LOOP
 CMP R0,#0
 MOVEQ R2,R1
 BEQ LC
 CMP R1,#0
 MOVEQ R2,R0
 BEQ LC
 CMP R1,R0
 MOVEQ R2,R1
 CMP R1,R0
 BGT BIG
 SUB R0,R0,R1
 
 B LOOP
 
 
LC
	CMP R4,#0
	SUBGT R4,R4,R2
	ADDGT R5,R5,#1
	B LC

BIG
 	SUB R1,R1,R0
	B LOOP

  END
